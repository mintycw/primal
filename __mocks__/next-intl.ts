export const useTranslations = () => (key: string) => key;
export const useFormatter = () => (value: any) => value;
export const NextIntlClientProvider = ({ children }: { children: React.ReactNode }) => children;
